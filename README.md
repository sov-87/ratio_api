ruby
----
`bundle install`

Then run sidekiq to aquire raios.

`sidekiq`

`rails s`

Build frontend
--------------
`cd frontend`
`yarn`
`npx webpack`

See result
----------
http://localhost:3000/ratio_info.html
